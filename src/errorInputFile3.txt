program FlujoIncorrecto;
var
  numero: integer;

begin
  numero := 10;

  if numero > 5 then
    write('El numero es mayor a 5');
  else
    write('El numero es menor o igual a 5');  { Error de sintaxis: else sin 'if' }

  for i := 1 to 10 do    { Error: Variable i no está declarada }
    write(i);
end.